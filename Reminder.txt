wut if je veux changer de pipeline mon material
or
if its not a basic pipeline

le addImporter peut creer une map d'id qui pointe vers des fonctions d'import -- La gestion des priority ? 
{
	priority	id of class
	{0,       10}
}

Max pipeline reach

je dois refaire les pipelines

AddPipeline if already exist

Surcouche a la gestion des pipelines pour update etc

Add SetTexture at Material class

Light struct is not alignas for gpu alignement

put TypeId au ileu de uint32_t


tu peux templaté les Shader/Compute Material;

reimplement input

_created pipeline != _computePIpeline

POur les materiaix j'ai plusisuers options :
 - struct a la malloc
 - Variant
 - rien de special et je garde mes un_map


////////// Brain Function //////////

le type de rendu doit etre instncié sur gpu et ya que transform

Donc, Je dis si c'est une mesh si ui je dois malloc ma mesh sur gpu, je le print pour le nombre que j'en ai, avec des transform different. 
ca c'est qui qui s'en occupe (gen le tout et les transforms) ?
C'est le compute Shader
pour l'affichage comment ca se passe ?
a la base on pourrait penseer qu'on render les objecct instantié mais ca pue,
le mieux serait de de gen les transform dans le compute shader avec la mesh et ca reviens pas a faire un mesh enable to render ?

donc vu que la mesh ne doit pas etre consideré comme un component d'Object, elle n'a pas d'owner et donc ne peux pas etre utilisé avec enablerenderer 
donc faut faire ca a la mano

///////// End Brain MalFunction /////////
