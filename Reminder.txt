Importer
{
	uint32_t import(std::string path);
}

MeshImporter : Importer 
{

}

RessourceImporter
{
	GetGoodImp()
	{

	}

	template<typename T>
	T Load(std::string path)
	{
		Importer* imp = GetGoodImp<T>();
		uint32_t yey = imp.import(path);
		unordered_map<string, T> map = RessourceMap<T>::GetMap();
		if (yey exist in map)
			return (map["path"]);
	}
}


Check si deja load
::
Siffit de check la string avec le type au bebut et si ya on return ca.
